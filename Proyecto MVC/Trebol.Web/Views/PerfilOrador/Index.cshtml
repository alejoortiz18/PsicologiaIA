@model IEnumerable<Trebol.Model.DTOs.Sala.SalaDto>
@{
    ViewData["Title"] = "Perfil de Orador";
    Layout = "~/Views/Shared/_LayoutInterno.cshtml";
    var profesionalId = ViewBag.ProfesionalId as int? ?? 0;
}

<h1 style="font-family:var(--font-display);color:var(--color-primary);font-size:1.5rem;margin-bottom:var(--sp-6);">🎤 Salas del profesional</h1>

@if (!Model.Any())
{
    <div class="card" style="padding:var(--sp-12);text-align:center;">
        <div style="font-size:3rem;margin-bottom:var(--sp-4);">🎬</div>
        <div style="font-weight:600;color:var(--color-text-primary);">Este profesional no tiene salas activas</div>
    </div>
}
else
{
    <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:var(--sp-5);">
        @foreach (var sala in Model)
        {
            <div class="card" style="overflow:hidden;">
                @if (!string.IsNullOrEmpty(sala.ImagenUrl))
                {
                    <img src="@sala.ImagenUrl" style="width:100%;height:160px;object-fit:cover;" alt="sala" />
                }
                else
                {
                    <div style="height:90px;background:linear-gradient(135deg,var(--color-primary),var(--color-primary-light));display:flex;align-items:center;justify-content:center;font-size:2.5rem;">🎬</div>
                }
                <div style="padding:var(--sp-5);">
                    <div style="font-weight:700;color:var(--color-text-primary);margin-bottom:6px;">@sala.Titulo</div>
                    <div style="font-size:.8rem;color:var(--color-text-muted);margin-bottom:var(--sp-3);">@sala.Descripcion</div>
                    <div style="display:flex;gap:var(--sp-2);flex-wrap:wrap;margin-bottom:var(--sp-4);">
                        <span class="badge badge-primary">@sala.Tipo</span>
                        <span class="badge @(sala.Estado.ToString() == "Abierta" ? "badge-success" : "badge-muted")">@sala.Estado</span>
                    </div>
                    <div style="display:flex;align-items:center;justify-content:space-between;">
                        <span style="font-size:.8rem;color:var(--color-text-muted);">👥 @sala.TotalInscritos / @sala.Capacidad</span>
                        <a asp-controller="Salas" asp-action="Detalle" asp-route-id="@sala.SalaId" class="btn btn-primary btn-sm">Ver sala</a>
                    </div>
                </div>
            </div>
        }
    </div>
}
